// Home page — full map view.
// The map and markers are rendered by the (map)/layout.tsx via MapShell.
// This page provides no overlay content.
export default function MapPage(): null {
  return null;
}
